name: Icons build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Build icons (svgo + sprite)
        run: npm run icons:build
      - name: Upload sprite artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: icons-sprite
          path: icons/sprite.min.svg
